<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for network and media access -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- For Android 13+ (SDK 33), optional for images/videos/files -->
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        tools:targetApi="33" />
    <uses-permission
        android:name="android.permission.READ_MEDIA_VIDEO"
        tools:targetApi="33" />

    <!-- Camera permission -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Corresponding hardware feature declaration -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name="MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GrindSphere">

        <!-- LoginActivity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.GrindSphere"
            android:launchMode="singleTop" />

        <!-- MainActivity as launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.GrindSphere">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AdminActivity -->
        <activity
            android:name=".AdminActivity"
            android:exported="true"
            android:label="Admin" />

        <!-- SignupActivity -->
        <activity
            android:name=".SignupActivity"
            android:exported="false"
            android:label="Sign Up"
            android:theme="@style/Theme.GrindSphere" />

        <!-- Hustler Activities -->
        <activity
            android:name=".hustler.HustlerDashboardActivity"
            android:exported="false"
            android:label="Hustler Dashboard"
            android:theme="@style/Theme.GrindSphere" />

        <activity
            android:name=".hustler.EditServiceActivity"
            android:exported="false" />

        <activity
            android:name=".hustler.NewChatActivity"
            android:exported="false"
            android:label="New Chat"
            android:theme="@style/Theme.GrindSphere" />

        <activity
            android:name=".hustler.ChatActivity"
            android:exported="false"
            android:label="Chat"
            android:theme="@style/Theme.GrindSphere" />

        <activity android:name=".hustler.HomePageActivity" />
        <activity
            android:name=".hustler.ServiceDetailActivity"
            android:exported="false" />

        <!-- Customer Activities -->
        <activity
            android:name=".customer.CustomerDashboardActivity"
            android:exported="false"
            android:label="Customer Dashboard"
            android:theme="@style/Theme.GrindSphere" />

    </application>
</manifest>
